
package assignment;

import java.time.LocalDate;


public class Storage {
    
        /*
        User u1 = new User("arman123", "pass", "arman", 25, Gender.Male, 176, 80.0);
        DietRecord d1 = new DietRecord(LocalDate.now(), u1, 1500);
        DietRecord d2 = new DietRecord(LocalDate.now(), u1, 500);
        u1.recommendedDailyIntake(User.ActivityLevel.Moderate, User.WeightGoal.Lose);
        u1.getBMI();
        ActivityRecord a1 = new ActivityRecord(LocalDate.now(), u1, 10000);
        ActivityRecord a2 = new ActivityRecord(LocalDate.now(), u1, 3000);
        a1.caloriesBurned();
        a2.caloriesBurned();
        u1.addRecord(a1);
        u1.addRecord(d1);
        u1.addRecord(a2);
        u1.addRecord(d2);
        u1.totalCaloriesBurnedToday();
        u1.displayAllRecords();
        u1.totalCalorieIntakeToday();
    */
}
